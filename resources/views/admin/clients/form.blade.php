<x-app-layout><x-slot name="header"><h1 class="text-2xl font-bold">{{ $item->exists?'Editar cliente':'Novo cliente' }}</h1></x-slot><form method="POST" action="{{ $item->exists?route('clients.update',$item):route('clients.store') }}" class="mx-auto max-w-2xl space-y-5 rounded-2xl border bg-white p-6">@csrf @if($item->exists)@method('PUT')@endif<label class="block text-sm font-semibold">Nome<input name="name" required value="{{ old('name',$item->name) }}" class="mt-1 w-full rounded-xl border-slate-300"></label><label class="block text-sm font-semibold">Descrição<textarea name="description" class="mt-1 w-full rounded-xl border-slate-300">{{ old('description',$item->description) }}</textarea></label><label class="flex items-center gap-2"><input type="hidden" name="active" value="0"><input type="checkbox" name="active" value="1" @checked(old('active',$item->exists?$item->active:true))> Ativo</label><button class="rounded-xl bg-cyan-500 px-5 py-3 font-bold">Salvar</button></form></x-app-layout>
